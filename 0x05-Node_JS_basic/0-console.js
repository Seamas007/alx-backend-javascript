/**
 * Displays a message that print in STDOUT.
 * @param {String} msg The message to display.
 * @author Adefemi Oreyomi <https://github.com/seamas007>
 */
const displayMessage = (msg) => {
  console.log(msg);
};

module.exports = displayMessage;
